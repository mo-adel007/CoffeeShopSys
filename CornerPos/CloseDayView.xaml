<UserControl x:Class="CornerPos.CloseDayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="{StaticResource AppFont}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Close Day" FontSize="22" FontWeight="Bold"
                   Foreground="{StaticResource EspressoBrush}" Margin="0,0,0,18"/>

        <!-- Total cards: Income, Expenses, Profit -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource CardBrush}" CornerRadius="16"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="INCOME" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource MutedBrush}"/>
                    <TextBlock x:Name="IncomeText" Text="0.00" FontSize="26" FontWeight="Bold"
                               Foreground="{StaticResource CaramelBrush}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource CardBrush}" CornerRadius="16"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="EXPENSES" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource MutedBrush}"/>
                    <TextBlock x:Name="ExpensesText" Text="0.00" FontSize="26" FontWeight="Bold"
                               Foreground="{StaticResource MochaBrush}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Background="{StaticResource EspressoBrush}" CornerRadius="16"
                    BorderThickness="0" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="PROFIT" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource CreamBrush}"/>
                    <TextBlock x:Name="ProfitText" Text="0.00" FontSize="26" FontWeight="Bold"
                               Foreground="White" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Two grids: Sales / Purchases -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" Text="Sales" FontSize="15" FontWeight="SemiBold"
                           Foreground="{StaticResource EspressoBrush}" Margin="2,0,0,8"/>
                <DataGrid x:Name="SellGrid" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="True"/>
            </DockPanel>

            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top" Text="Purchases" FontSize="15" FontWeight="SemiBold"
                           Foreground="{StaticResource EspressoBrush}" Margin="2,0,0,8"/>
                <DataGrid x:Name="BuyGrid" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="True"/>
            </DockPanel>
        </Grid>

        <!-- Close day button -->
        <Button x:Name="CloseDayBtn" Grid.Row="3" Content="Close day" Width="220" HorizontalAlignment="Right"
                Style="{StaticResource PrimaryButton}" Margin="0,20,0,0" Click="CloseDay_Click"/>
    </Grid>
</UserControl>
